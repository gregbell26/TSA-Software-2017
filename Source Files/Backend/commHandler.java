package Backend;
// a sloppy way to get all the things form the main method
public class commHandler {
    public static void commHandler(String userIn){
        if (userIn.equals("caesar")) {
            //CaesarCypher caesarCypher = new CaesarCypher(keyIn, global.textIn);
        }
    }
}
